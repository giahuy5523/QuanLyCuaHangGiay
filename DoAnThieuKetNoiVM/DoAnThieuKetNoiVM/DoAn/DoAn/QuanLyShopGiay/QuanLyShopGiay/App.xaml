<Application x:Class="QuanLyShopGiay.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:QuanLyShopGiay.Helpers"
             StartupUri="Views/Pages/HoaDonPage.xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>

        <!-- Converter 1: chuỗi rỗng → Collapsed | có nội dung → Visible -->
        <!-- Dùng: hiện/ẩn TextBlock thông báo lỗi ở Login, SignUp -->
        <helpers:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>

        <!-- Converter 2: bool → Visibility -->
        <!-- Dùng: ẩn/hiện nút theo phân quyền (Visibility) -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Converter 3: bool → Opacity (1.0 / 0.4) -->
        <!-- Dùng: mờ nút sidebar khi không có quyền -->
        <helpers:BoolToOpacityConverter x:Key="BoolToOpacityConverter"/>

        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFD1D9E0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF2D3E50"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF3D5166"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
