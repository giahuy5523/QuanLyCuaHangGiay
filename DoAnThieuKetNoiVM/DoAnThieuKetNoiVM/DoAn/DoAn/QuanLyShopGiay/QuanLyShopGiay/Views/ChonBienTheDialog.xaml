<Window x:Class="QuanLyShopGiay.Views.Dialogs.ChonBienTheDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chọn Size / Màu Sắc" Height="360" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="lblTenSP" Grid.Row="0" FontSize="15" FontWeight="Bold"
                   Foreground="#1E2A38" Margin="0,0,0,8" TextWrapping="Wrap"/>

        <TextBlock Grid.Row="1" Text="Chọn size và màu sắc:" FontSize="12"
                   Foreground="#7F8C8D" Margin="0,0,0,8"/>

        <ListBox x:Name="lbBienThe" Grid.Row="2" BorderBrush="#E0E0E0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!-- DataContext là SanPham, dùng đúng tên cột DB -->
                    <Grid Margin="4,6" Width="300">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="70"/>
                        </Grid.ColumnDefinitions>

                        <!-- Size - MauSac -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="Size " FontSize="13" Foreground="#2C3E50"/>
                            <TextBlock Text="{Binding Size}" FontSize="13" 
                                       FontWeight="SemiBold" Foreground="#2C3E50"/>
                            <TextBlock Text=" - " FontSize="13" Foreground="#7F8C8D"/>
                            <TextBlock Text="{Binding MauSac}" FontSize="13" Foreground="#2C3E50"/>
                        </StackPanel>

                        <!-- Giá bán -->
                        <TextBlock Grid.Column="1" 
                                   Text="{Binding GiaBan, StringFormat={}{0:N0}₫}"
                                   FontSize="12" Foreground="#E74C3C" 
                                   VerticalAlignment="Center" TextAlignment="Right"/>

                        <!-- Còn lại -->
                        <TextBlock Grid.Column="2" 
                                   Text="{Binding SoLuongTon, StringFormat='Còn: {0}'}"
                                   FontSize="11" Foreground="#27AE60"
                                   VerticalAlignment="Center" TextAlignment="Right"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Hủy" Width="80" Height="34" Margin="0,0,8,0"
                    Background="#F5F5F5" BorderBrush="#E0E0E0"
                    Click="BtnHuy_Click"/>
            <Button Content="Thêm vào giỏ" Width="120" Height="34"
                    Background="#27AE60" Foreground="White" 
                    BorderThickness="0" Click="BtnXacNhan_Click"/>
        </StackPanel>
    </Grid>
</Window>